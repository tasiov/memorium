<%= form_for([@user, @memorial], url: {action: "create"}) do |f| %>
  <% if @memorial.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@memorial.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @memorial.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>